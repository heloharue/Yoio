1. Dado const nums = [1, 2, 3, 4], escreva uma linha que produza [2, 4, 6, 8].
    const nums = [2, 4, 6, 8];

2. Na mesma lista, escreva uma linha que produza só os números pares.
    const pares = lista.filter(numero => numero % 2 === 0);

3. Na mesma lista, escreva uma linha que produza só os números pares.
    MAP: pega um item aplica uma função, e retorna a mesma lista, sem excluir anterior.
    FILTER: pega uma lisa impoem uma condição, retorna uma lista apenas com os items que passaram nesse iltro.

4. Dado const p = { titulo: "Caneca", preco: 25 }, desestruture titulo e preco em uma linha.    
    const {titulo, preco} = p;    

5. Escreva uma função de seta chamada ehCaro que recebe um número e devolve verdadeiro se for maior
que 100.
    const ehCaro = preco => preco . 100 

6. Dado const cores = ["azul", "verde"], crie cores2 com "vermelho"no fim, sem alterar cores.
    const cores2 = [...cores, "vermelho'];

7. Dado o objeto p da questão 4, crie pEmPromocao com preço 20, sem alterar p.
    const pEmPromocao = {...p, preco : 20 }



8. Dada a lista de produtos abaixo, escreva uma linha que devolva só os nomes dos produtos em estoque.
const produtos = [
{ nome: "Caneca", estoque: 3 },
{ nome: "Camiseta", estoque: 0 },
{ nome: "Adesivo", estoque: 7 }
];



const produtos

9. O que está errado neste código? const total = precos.map((p) => { p * 2 });
10. Qual a diferença entre import Botao from "./Botao.js" e import { Botao } from "./Botao.js"?
11. Explique por que lista.push(novo) é um problema dentro de um componente React.
12. Escreva uma linha que, a partir de produtos, devolva os nomes apenas dos que têm estoque maior que 5.

