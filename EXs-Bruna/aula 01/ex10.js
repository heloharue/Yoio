//O primeiro (import Botao) importa o componente que foi exportado como padrão (export default), enquanto o segundo (import { Botao }) importa uma exportação nomeada específica (export const Botao).
